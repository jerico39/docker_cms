設定したユーザー
user
wakai39@gmail.com
test22


用語の指すファイルの保管場所
・マイグレーション
database/migrations/

・モデル
app/Models/

・Resource
app/Filament/Resources

WSL内のコンテナで作成したファイルを更新できない場合、
そのファイルがあるディレクトリに移動して以下のコマンドでユーザーを1000に変更したら、変更できた。

chown -R 1000:1000 *

参考元
https://chatgpt.com/share/681da2da-6678-8009-b61e-780dc51fe646

https://zenn.dev/egstock_inc/articles/6d07a13bdc59a3




https://chatgpt.com/c/6996820a-fa90-8320-9b3c-22694cc47ef4


何やっても500エラー問題。
laravel.logも更新されない場合。
logを掴んでないか確認。

root@b1c348413ec6:/var/www/cms# chown -R 1000:1000 *
root@b1c348413ec6:/var/www/cms# chmod -R 777 storage
root@b1c348413ec6:/var/www/cms# chmod -R 777 bootstrap/cache




ファイル更新後のキャッシュクリア
・コンテナから
php artisan route:clear
php artisan cache:clear
php artisan config:clear


※全部のキャッシュア
php artisan optimize:clear



dockerのバッシュから
docker compose exec app php artisan route:clear

※
Cドライブ以下でディレクトリを作成し実行した。
※WSL以下だと、.envの変更がどうしてもできなくなる。

コンテナに入る。
winpty docker exec -it docker_cms-app-1 bash



インストール
composer create-project laravel/laravel cms


.env　のDB変更




cd cms

・設定更新
php artisan config:clear

TOPに接続

■エラー
tempnam(): file created in the system's temporary directory

＞以下のコマンドで解消
chown -R www-data:www-data /var/www/cms/storage /var/www/cms/bootstrap/cache
chmod -R 775 /var/www/cms/storage /var/www/cms/bootstrap/cache

■エラー
Illuminate\Database\QueryException
vendor/laravel/framework/src/Illuminate/Database/Connection.php:838
SQLSTATE[42S02]: Base table or view not found: 1146 Table 'laravel.sessions' doesn't exist (Connection: mysql, Host: db, Port: 3306, Database: laravel, SQL: select * from `sessions` where `id` = f0WaXUPKUQTyhe2LBp7sNDyvo10RoLKXY38a9pFa limit 1)

＞sessions テーブルを作るコマンド
php artisan session:table
php artisan migrate


→TOPが見えるようになる。


■filament パッケージのインストール
composer require filament/filament:"^3.2" -W

■パネル（管理画面）のセットアップ:
php artisan filament:install --panels
↓管理画面のURL
 What is the ID? ─────────────────────────────────────────────┐
 │ admin    


■ユーザーの作成
php artisan make:filament-user

設定したユーザー
user
wakai39@gmail.com
test22

http://localhost:8000/admin/

で管理画面ログイン画面が表示


チャッピー
https://chatgpt.com/c/699fb11b-78d8-8323-aeb0-8c9347a1e15b


以下から、企業型CMSの作成手順
■
① Page（固定ページ管理）
例：
about（会社概要）
service（事業内容）
recruit（採用）

php artisan make:model Page -m


database/migrations/
以下にファイルが自動生成されるので、その中に定義を追記する。
$table->id();
$table->string('title');
$table->string('slug')->unique();
$table->longText('content')->nullable();
$table->boolean('is_published')->default(true);
$table->timestamps();


以下を実行でテーブル作成（全テーブル定義を書いてから実行してもよい）
php artisan migrate








③ SiteSetting（サイト共通情報）
php artisan make:model SiteSetting -m

作成されたファイルへの定義
       $table->id();

        // 会社情報
        $table->string('company_name');
        $table->string('address')->nullable();
        $table->string('phone')->nullable();
        $table->string('email')->nullable();

        $table->timestamps();

反映
php artisan migrate


■Filament Resource作成
php artisan make:filament-resource Page
php artisan make:filament-resource News
php artisan make:filament-resource SiteSetting

これかコマンドで
app/Filament/Resources/●●●Resource.php]
のファイルが作成される。


企業CMSで必須カスタマイズ
Page Resource の form 例、public static function form(Form $form): Form
の中の
        ->schema([
                //
            ]);
 内に以下を追記する。
 

Forms\Components\TextInput::make('title')->required();

Forms\Components\TextInput::make('slug')
    ->required()
    ->unique(ignoreRecord: true);

Forms\Components\RichEditor::make('content')
    ->columnSpanFull();

Forms\Components\Toggle::make('is_published');


② フロント側表示の Route はどこに書く？
📍 ファイルの場所
routes/web.php


に以下を追記
Route::get('/{slug}', function ($slug) {
    $page = Page::where('slug', $slug)
        ->where('is_published', true)
        ->firstOrFail();

    return view('pages.show', compact('page'));
});



■
resources/views/layouts/app.blade.php
を作成


<内容>

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>@yield('title')</title>
</head>
<body>

<header>
    <h1>株式会社サンプル</h1>
    <nav>
        <a href="/">TOP</a>
        <a href="/about">会社概要</a>
        <a href="/service">事業内容</a>
        <a href="/contact">お問い合わせ</a>
    </nav>
</header>

<main>
    @yield('content')
</main>

<footer>
    <p>© 2026 株式会社サンプル</p>
</footer>

</body>
</html>

●
Step2：show.blade.php をレイアウト対応に変更
resources/views/pages/show.blade.php

＜内容＞

@extends('layouts.app')

@section('title', $page->title)

@section('content')
    <h2>{{ $page->title }}</h2>

    <div>
        {!! $page->content !!}
    </div>
@endsection

●
Step3：TOPページをCMS化

\routes\web.php にTOP用追加（推奨）

use App\Models\Page;

Route::get('/', function () {
    $page = Page::where('slug', 'home')
        ->where('is_published', true)
        ->firstOrFail();

    return view('pages.show', compact('page'));
});



※ /{slug} の上に書いてください。



・C:\docker_cms\src\cms\routes\web.php
の以下をコメントアウト
#Route::get('/', function () {
#    return view('welcome');
#});


■Pageモデルの変更
C:\docker_cms\src\cms\app\Models\Page.php

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Page extends Model
{
    protected $fillable = [
        'title',
        'slug',
        'content',
        'is_published',
    ];
}


↑これが無いと管理画面でPage登録時に以下のエラーが出る。
Illuminate\Database\Eloquent\MassAssignmentException
vendor/laravel/framework/src/Illuminate/Database/Eloquent/Model.php:624
Add [title] to fillable property to allow mass assignment on [App\Models\Page].


■管理画面Pageの一覧画面の表示項目。

C:\docker_cms\src\cms\app\Filament\Resources\PageResource.php
の
public static function table(Table $table): Table
にある。　->columns の内容を以下に変更。

->columns([
            Tables\Columns\TextColumn::make('title')
                ->searchable()
                ->sortable(),

            Tables\Columns\TextColumn::make('slug')
                ->searchable(),

            Tables\Columns\IconColumn::make('is_published')
                ->boolean(),

            Tables\Columns\TextColumn::make('updated_at')
                ->dateTime('Y-m-d H:i'),
        ])
        ->defaultSort('updated_at', 'desc')
        
        



■Pageの登録
・管理画面にて、
メニューのPage、New PAGEで新規ページを追加


slugは「home」
Is publishedをON

→TOPが表示される。

http://localhost:8000/





■Pageの登録にて画像が登録されなかった。
Pageを作成する各要素に以下を追加する

×・マイグレーション(カラム追加マイグレーションに変更)
×$table->string('image')->nullable();


カラム追加用マイグレーション
実行して、
php artisan make:migration add_image_to_pages_table --table=pages


作成されたファイルの中身は
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pages', function (Blueprint $table) {
            $table->string('image')->nullable()->after('content');
        });
    }

    public function down(): void
    {
        Schema::table('pages', function (Blueprint $table) {
            $table->dropColumn('image');
        });
    }
};


マイグレーション実行で、imageフィールド追加
php artisan migrate



・モデル
protected $fillable = [
    'title',
    'slug',
    'content',
    'is_published',
    'image',
];



storageリンク確認（まだなら）
php artisan storage:link



・Resource
PageResource.php の form にこう書いていますか？

Forms\Components\FileUpload::make('image')
    ->image()
    ->directory('pages')
    ->disk('public'),






追加したらコンテナ内からコマンドで反映
php artisan storage:link
このコマンドでstorage/app/publicがpublic/storage に公開される。




※ cms/public ls -l でシンボリックリンクになっていることがわかる。

storage -> /var/www/cms/storage/app/public




表示されない
直接パスでも画像が表示されない。

例
http://localhost:8000/storage/pages/01KJEYDVTVDTGMRJT56EADMT8N.jpg





権限
docker compose exec app chmod -R 775 storage
docker compose exec app chmod -R 775 bootstrap/cache


これでも見れないと
ls -la storage/app/public/pages
で,root権限になってないか見てみる。

もしroot権限なら権限変更。
chown -R www-data:www-data storage




■管理画面で画像のloadが終わらない

.env の設定を修正
APP_URL=http://localhost:8000

違っていたら修正 → コンテナ再起動：

docker compose restart





指示にしたがってあちこち修正したあとに、

ls -l public/storage
をうつと、 public/storage
ls: cannot access 'public/storage': No such file or directory


なので以下のコマンドでシンボリンクリンクを設定
php artisan storage:link



ls -l public/storage
以下のように帰ってきて、画像が見れるようになった。
public/storage -> /var/www/cms/storage/ap
p/public








■NEWSを作成、管理画面

php artisan make:model News -m
php artisan make:model Category -m
php artisan make:model Tag -m




② News（お知らせ）
php artisan make:model News -m

database/migrations/
以下にファイルが自動生成されるので、その中に定義を追記する。
$table->id();
$table->string('title');
$table->string('slug')->unique();
$table->text('content');
$table->timestamp('published_at')->nullable();
$table->boolean('is_published')->default(false);
$table->timestamps();






WSL内のコンテナで作成したファイルを更新できない場合、
そのファイルがあるディレクトリに移動して以下のコマンドでユーザーを1000に変更したら、変更できた。
chown -R 1000:1000 *




php artisan make:model Category -m


Schema::create('categories', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('slug')->unique();
    $table->timestamps();
});



php artisan make:model Tag -m

Schema::create('tags', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('slug')->unique();
    $table->timestamps();
});


■news_tag（中間テーブル）

php artisan make:migration create_news_tag_table


chown -R 1000:1000 database/migrations/*


Schema::create('news_tag', function (Blueprint $table) {
    $table->foreignId('news_id')->constrained()->cascadeOnDelete();
    $table->foreignId('tag_id')->constrained()->cascadeOnDelete();
});



・マイグレーションファイルをDBに反映
php artisan migrate




■モデルのリレーション
News.php

class News extends Model
{
    protected $fillable = [
        'title',
        'slug',
        'content',
        'image',
        'is_published',
        'published_at',
        'category_id',
    ];

    public function category()
    {
        return $this->belongsTo(Category::class);
    }

    public function tags()
    {
        return $this->belongsToMany(Tag::class);
    }
}





・Category.php
class Category extends Model
{
    protected $fillable = ['name', 'slug'];

    public function news()
    {
        return $this->hasMany(News::class);
    }
}




・Tag.php
class Tag extends Model
{
    protected $fillable = ['name', 'slug'];

    public function news()
    {
        return $this->belongsToMany(News::class);
    }
}



■ Filament Resource 作成
php artisan make:filament-resource News
php artisan make:filament-resource Category
php artisan make:filament-resource Tag




・NewsResource form()
app/Filament/Resources/NewsResource.phpの以下に追記

public static function form(Form $form): Form
{
    return $form
        ->schema([
            // 👈 ここに書きます
        ]);
}

以下を「ここに書きます」に追記

Forms\Components\TextInput::make('title')
    ->required(),

Forms\Components\TextInput::make('slug')
    ->required()
    ->unique(ignoreRecord: true),

Forms\Components\Select::make('category_id')
    ->relationship('category', 'name')
    ->searchable()
    ->preload(),

Forms\Components\Select::make('tags')
    ->multiple()
    ->relationship('tags', 'name')
    ->preload(),

Forms\Components\FileUpload::make('image')
    ->image()
    ->disk('public')
    ->directory('news')
    ->visibility('public')
    ->nullable(),

Forms\Components\RichEditor::make('content')
    ->columnSpanFull(),

Forms\Components\Toggle::make('is_published'),

Forms\Components\DateTimePicker::make('published_at'),




■フロント表示例

TOPに最新Newsを出すなら：

routes/web.php  の上部に以下を追加


use App\Models\News;

加えて

Route::get('/', function () 　の中のreturnより上に以下を追加

    $news = News::where('is_published', true)
        ->orderByDesc('published_at')
        ->take(5)
        ->get();
        


■次に Blade に表示を書く

resources/views/top.blade.php

次に Blade に表示を書く

<h2>Latest News</h2>

@foreach($news as $item)
    <div style="margin-bottom:20px;">
        <h3>{{ $item->title }}</h3>

        @if($item->image)
            <img src="{{ asset('storage/' . $item->image) }}"
                 style="max-width:200px;">
        @endif

        <p>{{ \Illuminate\Support\Str::limit(strip_tags($item->content), 100) }}</p>
    </div>
@endforeach




■カテゴリ登録
app/Filament/Resources/CategoryResource.php
の
public static function form(Form $form): Form
に以下を記述
    return $form
        ->schema([
            Forms\Components\TextInput::make('name')
                ->required()
                ->maxLength(255),

            Forms\Components\TextInput::make('slug')
                ->required()
                ->unique(ignoreRecord: true),
        ]);
        
        

モデルの fillable
app/Models/Category.php のclass Category extends Model 内に以下を追記

protected $fillable = [
    'name',
    'slug',
];


↑これらを■タグ登録も同様に行う。




＠カテゴリとタグを登録し、それらを使ってニュースを登録すると以下のエラー


# Illuminate\Database\QueryException - Internal Server Error

SQLSTATE[42S22]: Column not found: 1054 Unknown column 'category_id' in 'field list' (Connection: mysql, Host: db, Port: 3306, Database: laravel, SQL: insert into `news` (`title`, `slug`, `category_id`, `image`, `content`, `is_published`, `published_at`, `updated_at`, `created_at`) values (３月２日のニュース, news0302, 1, news/01KJP6ZEF9BTC2NC1519GKRNK9.png, <p>２０２６年３月２日のニュースです。</p><p>大変なことがおきました。これはすごいです。革命的です。</p><p>世界初のこころみです。</p><p><br></p>, 0, ?, 2026-03-02 02:45:29, 2026-03-02 02:45:29))



→DBにcategory_id のフィールドが無かった



①マイグレーションでフィールド追加
php artisan make:migration add_category_id_to_news_table

※追加するたびに必要っぽいｎ
 chown -R 1000:1000 database/*


コマンドで作成したマイグレーションファイルに以下を記載する。



Schema::table('news', function (Blueprint $table) {
    $table->foreignId('category_id')
          ->nullable()
          ->constrained()
          ->nullOnDelete();
});



②ニュースにimageフィールドも無いので追加」
php artisan make:migration add_image_to_news_table

$table->string('image')->nullable()->after('content');



NewsResourceにも追加
            Forms\Components\FileUpload::make('image')
                ->image()   //拡張子が    jpg、png、webp、gif
                ->disk('public')
                ->directory('pages')
                ->visibility('public')
                ->preserveFilenames() //リネーム防止
                ->nullable()
                ->dehydrated(fn ($state) => filled($state)),
                




■Newsの管理画面にて一覧で項目が表示されない。

app/Filament/Resources/NewsResource.php
に

テーブルを追加
use Filament\Tables;
use Filament\Tables\Table;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Columns\ImageColumn;
use Filament\Tables\Columns\IconColumn;


クラス内に表示を追加

public static function table(Table $table): Table
{
    return $table
        ->columns([
            ImageColumn::make('image')
                ->label('画像')
                ->square(),

            TextColumn::make('title')
                ->label('タイトル')
                ->searchable()
                ->sortable(),

            TextColumn::make('category.name')
                ->label('カテゴリ')
                ->sortable(),

            IconColumn::make('is_published')
                ->label('公開')
                ->boolean(),

            TextColumn::make('published_at')
                ->label('公開日')
                ->dateTime('Y-m-d')
                ->sortable(),

            TextColumn::make('created_at')
                ->label('作成日')
                ->dateTime('Y-m-d'),
        ])
        ->defaultSort('created_at', 'desc');
}





・リレーションを確認
app/Models/News.php

以下をいれないとcategory.name が表示されない

public function category()
{
    return $this->belongsTo(Category::class);
}




・修正
一覧が表示されましたが、in publis に設定していれても公開日が表示されません。 公開日はin publish がONになった日付を入るようにしてください。
↓

■自動で公開日を入れる方法１


モデルに booted() を追加します。



use Illuminate\Support\Carbon;

protected static function booted()
{
    static::saving(function ($news) {

        if ($news->is_published && !$news->published_at) {
            $news->published_at = now();
        }

        if (!$news->is_published) {
            $news->published_at = null;
        }

    });
}





■自動で公開日を入れる方法２
form()にて以下を追加しても、公開した日に公開日がはいるらしい。
しかし、👉 モデル側でやる方が安全（APIやSeederでも動く）

Toggle::make('is_published')
    ->reactive()
    ->afterStateUpdated(function ($state, callable $set) {
        if ($state) {
            $set('published_at', now());
        } else {
            $set('published_at', null);
        }
    }),






■予約投稿を可能とする。

フィールド定義の変更	
php artisan make:migration add_publish_fields_to_news_table


※追加するたびに必要っぽいｎ
 chown -R 1000:1000 database/*
 
 モデル修正
 
 
 app/Models/News.php
 
 
 use Illuminate\Database\Eloquent\Builder;

public function scopePublished(Builder $query): Builder
{
    return $query
        ->where('is_published', true)
        ->whereNotNull('published_at')
        ->where('published_at', '<=', now());
}

 
 
 
 
 変更
 \\wsl.localhost\Ubuntu\home\docker_cms\src\cms\app\Models\News.php
 

protected static function booted()
{
    static::saving(function ($news) {

        // 公開ONだけど日時が未設定なら今
        if ($news->is_published && !$news->published_at) {
            $news->published_at = now();
        }

        // 公開OFFなら日時クリア
        if (!$news->is_published) {
            $news->published_at = null;
        }
    });
}




■TOPにニュースの表示


route/web.phpの変更
use App\Models\News;

Route::get('/', function () {
    $page = Page::where('slug', 'home')
        ->where('is_published', true)
        ->firstOrFail();
    $newsList = News::published()
        ->latest('published_at')
        ->take(5)
        ->get();

    return view('pages.show', compact('page', 'newsList'));
});


resources/views/pages/show.blade.php の変更

の好きなところに以下を追加
@if(isset($newsList) && $newsList->count())
    <section class="news-area">
        <h2>NEWS</h2>

        @foreach($newsList as $news)
            <div class="news-item">
                <p>{{ $news->published_at->format('Y-m-d') }}</p>
                <h3>{{ $news->title }}</h3>
            </div>
        @endforeach
    </section>
@endif




■ニュース詳細ページ
routes/web.php に以下を追加（/{slug} より上に！重要）)

use App\Models\News;

Route::get('/news/{slug}', function ($slug) {

    $news = News::published()
        ->where('slug', $slug)
        ->firstOrFail();

    return view('news.show', compact('news'));

})->name('news.show');



② 詳細ページのBlade作成

resources/views/news/show.blade.php を作成

<h1>{{ $news->title }}</h1>

<p>{{ $news->published_at->format('Y-m-d') }}</p>

@if($news->image)
    <img src="{{ asset('storage/' . $news->image) }}" width="400">
@endif

<div>
    {!! $news->content !!}
</div>






■管理画面にて、カテゴリ一覧とタグ一覧で項目を表示

Filament は

form() → 登録画面

table() → 一覧画面

です。

今は

👉 table() が未定義
👉 だから一覧に列が表示されない


カテゴリ
app/Filament/Resources/CategoryResource.php


① CategoryResource のファイル上部 table() を追加

use Filament\Tables;
use Filament\Tables\Table;
use Filament\Tables\Columns\TextColumn;



内部にテーブル追加

public static function table(Table $table): Table
{
    return $table
        ->columns([
            TextColumn::make('name')
                ->label('カテゴリ名')
                ->searchable()
                ->sortable(),

            TextColumn::make('slug')
                ->label('スラッグ')
                ->searchable(),

            TextColumn::make('created_at')
                ->label('作成日')
                ->dateTime('Y-m-d'),
        ])
        ->defaultSort('created_at', 'desc');
}





タグ
app/Filament/Resources/TagResource.php

もカテゴリと同じように追加。


public static function table(Table $table): Table
{
    return $table
        ->columns([
            TextColumn::make('name')
                ->label('タグ名')
                ->searchable()
                ->sortable(),

            TextColumn::make('slug')
                ->label('スラッグ'),

            TextColumn::make('created_at')
                ->label('作成日')
                ->dateTime('Y-m-d'),
        ])
        ->defaultSort('created_at', 'desc');
}


＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝













■管理にUserメニューを追加
① UserResource を作成

プロジェクトルートで実行：

php artisan make:filament-resource User

すると次のファイルが作られます：

app/Filament/Resources/UserResource.php
app/Filament/Resources/UserResource/Pages/ListUsers.php
app/Filament/Resources/UserResource/Pages/CreateUser.php
app/Filament/Resources/UserResource/Pages/EditUser.php

これでメニューに Userが追加される。

docker 上で作成したファイルをWin上で編集可能に
chown -R 1000:1000 *


■	Userフォームを整える
app/Filament/Resources/UserResource.php

form() をこう修正します。

use Filament\Forms;
use Filament\Forms\Form;

public static function form(Form $form): Form
{
    return $form
        ->schema([

            Forms\Components\TextInput::make('name')
                ->required()
                ->maxLength(255),

            Forms\Components\TextInput::make('email')
                ->email()
                ->required()
                ->unique(ignoreRecord: true),

            Forms\Components\TextInput::make('password')
                ->password()
                ->dehydrated(fn ($state) => filled($state))
                ->required(fn ($record) => $record === null),

        ]);
}




③ パスワードをハッシュ化
同ファイルに以下を追加

use Illuminate\Support\Facades\Hash;

password フィールドを変更：

Forms\Components\TextInput::make('password')
    ->password()
    ->dehydrateStateUsing(fn ($state) => Hash::make($state))
    ->dehydrated(fn ($state) => filled($state))
    ->required(fn ($record) => $record === null)





④ 一覧表示

use Filament\Tables;
use Filament\Tables\Table;

public static function table(Table $table): Table
{
    return $table
        ->columns([
            Tables\Columns\TextColumn::make('name')
                ->searchable(),

            Tables\Columns\TextColumn::make('email')
                ->searchable(),

            Tables\Columns\TextColumn::make('created_at')
                ->dateTime('Y-m-d'),
        ]);
}


■さらにUser機能を強化
① ユーザー権限カラム追加

まず users テーブルに role を追加します。

php artisan make:migration add_role_to_users_table


database/migrations/ にファイルが作成される

docker 上で作成したファイルをWin上で編集可能に
chown -R 1000:1000 *


Schema::table('users', function (Blueprint $table) {
    $table->string('role')->default('editor');
});


実行

php artisan migrate



② Userモデルに権限メソッド
app/Models/User.php


public function isAdmin()
{
    return $this->role === 'admin';
}


③ User管理画面にRole追加

app/Filament/Resources/UserResource.php

form() に追加

use Filament\Forms\Components\Select;

Select::make('role')
    ->options([
        'admin' => 'Admin',
        'editor' => 'Editor',
    ])
    ->required()
    
 
一覧表示

TextColumn::make('role')
    ->badge()
    ->color(fn ($state) => $state === 'admin' ? 'danger' : 'success')
    
    
   
   
④ Newsに投稿者追加

php artisan make:migration add_user_id_to_news_table

    


■サイト設定の追加

DB
php artisan make:model Setting -m

docker 上で作成したファイルをWin上で編集可能に
chown -R 1000:1000 *


テーブルの設定
public function up(): void
{
    Schema::create('settings', function (Blueprint $table) {
        $table->id();

        $table->string('site_name')->nullable();
        $table->string('site_description')->nullable();
        $table->string('logo')->nullable();
        $table->string('contact_email')->nullable();

        $table->timestamps();
    });
}


DBへ反映
php artisan migrate



3 初期データを作る（重要）
php artisan tinker
↑これで起動するプロンプトに以下のコマンドを実行する。
App\Models\Setting::create();

これで settings テーブルに1レコード作成されます。
※代わりにSeederを使うこともできる

説明：
https://webabc.hatenablog.jp/entry/2020/12/17/laravel-tinker-add-user


4 Filament Page を作成

php artisan make:filament-page SiteSetting

[app/Filament/Pages/SiteSetting.php] が作成される。

docker 上で作成したファイルをWin上で編集可能に
chown -R 1000:1000 *


5 Page を編集

<?php

namespace App\Filament\Pages;

use Filament\Pages\Page;
use Filament\Forms;
use Filament\Forms\Form;
use App\Models\Setting;
use Filament\Notifications\Notification;

class SiteSetting extends Page implements Forms\Contracts\HasForms
{
    use Forms\Concerns\InteractsWithForms;

    protected static ?string $navigationIcon = 'heroicon-o-cog-6-tooth';

    protected static ?string $navigationLabel = 'サイト設定';

    protected static ?string $navigationGroup = 'サイト管理';

    protected static string $view = 'filament.pages.site-setting';

    public ?array $data = [];

    public function mount(): void
    {
        $setting = Setting::first();

        $this->form->fill($setting->toArray());
    }

    public function form(Form $form): Form
    {
        return $form
            ->statePath('data')
            ->schema([

                Forms\Components\TextInput::make('site_name')
                    ->label('サイト名'),

                Forms\Components\TextInput::make('site_description')
                    ->label('サイト説明'),

                Forms\Components\TextInput::make('contact_email')
                    ->label('メール'),

                Forms\Components\FileUpload::make('logo')
                    ->label('ロゴ')
                    ->directory('site'),

            ]);
    }

	public function save()
	{
	    $setting = Setting::first();

	    $setting->update($this->form->getState());

	    Notification::make()
	        ->title('保存しました')
	        ->success()
	        ->send();
	}
}



6 Blade View 作成
resources/views/filament/pages/site-setting.blade.php


以下のように編集

<x-filament::page>

<form wire:submit.prevent="save">

    {{ $this->form }}

    <x-filament::button type="submit">
        保存
    </x-filament::button>

</form>

</x-filament::page>



7 管理画面に表示
/admin/site-setting


※Laravel の Mass Assignment　エラーの回避
Add [site_name] to fillable property to allow mass assignment on [App\Models\Setting].

Setting::update($data);

のような 一括保存を禁止しています。

許可するカラムを

$fillable

で指定します。


修正ファイル
app/Models/Setting.php

修正前
class Setting extends Model
{
    use HasFactory;
}

↓
修正後

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Setting extends Model
{
    protected $fillable = [
        'site_name',
        'site_description',
        'logo',
        'contact_email',
    ];
}








■フロントでの使いかあt

フロント側での使い方

例：ヘッダー
ファイル
resources/views/layouts/app.blade.php

@php
$setting = \App\Models\Setting::first();
@endphp

<title>{{ $setting->site_name }}</title>





■Menu（ナビゲーション管理） の実装方法を教えてください

Laravel + Filament で **Menu（ナビゲーション管理）**を実装する方法を、
**CMSでよく使う構成（実務レベル）**で説明します。

完成すると管理画面でこうなります。

メニュー
  メニュー一覧
     ├ ヘッダー
     └ フッター

メニュー項目
     ├ ホーム
     ├ ニュース
     ├ お問い合わせ

そしてフロントでは

Home | News | Contact

のように表示できます。

① Menuテーブル作成
php artisan make:model Menu -m

編集

database/migrations/xxxx_create_menus_table.php

chown -R 1000:1000 *

↓
Schema::create('menus', function (Blueprint $table) {
    $table->id();
    $table->string('name'); // ヘッダーなど
    $table->string('slug')->unique();
    $table->timestamps();
});



② MenuItemテーブル作成
php artisan make:model MenuItem -m

編集

database/migrations/xxxx_create_menu_items_table.php

chown -R 1000:1000 *

↓
Schema::create('menu_items', function (Blueprint $table) {
    $table->id();

    $table->foreignId('menu_id')->constrained()->cascadeOnDelete();

    $table->string('title'); // 表示名
    $table->string('url');   // リンク

    $table->integer('sort')->default(0);

    $table->timestamps();
});


③ migrate
php artisan migrate
↑DBにマイグレーションの定義でテーブル作成


④ Modelリレーション
app/Models/Menu.php


class MenuItem extends Model
{
    protected $fillable = [
        'menu_id',
        'title',
        'url',
        'sort',
        'name',
        'slug',
    ];

    public function menu()
    {
        return $this->belongsTo(Menu::class);
    }
}


※ここでフィールド名を設定しないと、メニュー等で保存した時に以下のようなエラーがでる。
SQLSTATE[HY000]: General error: 1364 Field 'name' doesn't have a default value (Connection: mysql, Host: db, Port: 3306, Database: laravel, SQL: insert into `menus` (`updated_at`, `created_at`) values (2026-03-11 07:09:00, 2026-03-11 07:09:00))





⑤ Filament Resource 作成

php artisan make:filament-resource Menu

↓これが作成される。
[app/Filament/Resources/MenuResource.php] 

以下でWInから編集可能に
chown -R 1000:1000 *



use Filament\Forms\Form;
use Filament\Forms\Components\TextInput;



public static function form(Form $form): Form
{
    return $form
        ->schema([
            TextInput::make('name')
                ->label('メニュー名')
                ->required(),

            TextInput::make('slug')
                ->label('スラッグ')
                ->required()
                ->unique(ignoreRecord: true),
        ]);
}







php artisan make:filament-resource MenuItem
↓が作成される。
 [app/Filament/Resources/MenuItemResource.php] 


public static function form(Form $form): Form
{
    return $form
        ->schema([
            Select::make('menu_id')
                ->relationship('menu', 'name')
                ->label('メニュー')
                ->required(),

            TextInput::make('title')
                ->label('タイトル')
                ->required(),

            TextInput::make('url')
                ->label('URL')
                ->required(),

            TextInput::make('sort')
                ->label('並び順')
                ->numeric()
                ->default(0),
        ]);
}





⑧ フロント側で表示

例：ヘッダー作成
resources/views/layouts/header.blade.php






===================================


composer require laravel/breeze --dev
php artisan breeze:install
npm install && npm run build
php artisan migrate







解消
WSLにログインして以下のコマンド
 sudo chmod -R 777 src/storage src/bootstrap/cache






✅ .env設定例（MySQL連携）
src/.env の一部を以下のように設定してください：


DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=secret


http://localhost:8000



エラー
SQLSTATE[42S02]: Base table or view not found: 1146 Table 'laravel.sessions' doesn't exist (Connection: mysql, SQL: select * from sessions where id = 1ArjTxo5RRn7JROOC03kShXvaV6BSAdUYtIUjjyr limit 1)


・docker_lara直下で以下のコマンド
※srcはドキュメントルート

docker 
composer create-project laravel/laravel cms 



コンテナに入る


winpty docker exec -it docker_lara-app-1 bash

権限の変更コマンド
chown -R www-data:www-data /var/www/cms/storage /var/www/cms/bootstrap/cache
chmod -R 775 /var/www/cms/storage /var/www/cms/bootstrap/cache


chown -R www-data:www-data /var/www/cms/.env
chmod -R 775 /var/www/cms/.env




Illuminate\Database\QueryException
vendor/laravel/framework/src/Illuminate/Database/Connection.php:838
SQLSTATE[HY000]: General error: 8 attempt to write a readonly database (Connection: sqlite, Database: /var/www/cms/database/database.sqlite, SQL: update "sessions" set "payload" = , "last_activity" = 1772003398, "user_id" = ?, "ip_address" = 172.30.0.1, "user_agent" = Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/145.0.0.0 Safari/537.36, "id" = dmltwQrhjxIuWvAFGnA0ebyzwMYYdeQIDgtjKVCC where "id" = dmltwQrhjxIuWvAFGnA0ebyzwMYYdeQIDgtjKVCC)


.envの設定を書き換える
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=secret



昔の
====================
解決：
docker-compose exec app php artisan session:table


# セッションテーブルのマイグレーションファイルを作成
docker-compose exec app php artisan session:table

# マイグレーションを実行してテーブルを作成
docker-compose exec app php artisan migrate

※
セッションをデータベースに保存する必要がなければ、.env を以下のようにして ファイルドライバに戻すこともできます：

env

SESSION_DRIVER=file

そのあと：

bash
docker-compose exec app php artisan config:clear





laraveのTOP画面が見える場外になる。


■管理画面 filamentのインストール

コンテナ内のlaravelディレクトリで以下のコマンドを実行
composer require filament/filament









AdminLTEのインストール
root@da0576abf265:/var/www# php artisan adminlte:install

root@da0576abf265:/var/www# php artisan adminlte:install --only=auth_views




ついでにやった(いるかいらんか不明)
php artisan key:generate
php artisan storage:link
chmod -R 777 storage bootstrap/cache
php artisan migrate



コマンド実行後、下記にファイルやディレクトリが生成されていればインストールは完了です。
src/config/adminlte.php
src/lang/
src/public/vendor/
src/resources/views/auth/


管理画面の表示
http://localhost:8000/admin/login


後のhomeの表示実装のURL
http://localhost:8000/admin/home
